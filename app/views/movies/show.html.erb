<h1><small>Movie`s </small><%= @movie.title %> page</h1>
<p>Year: <%= @movie.year %></p>

<h4>My Fans: </h4>
<ul>
  <% @movie.fans.each do |user| %>
    <li><%= user.name %></li>
    <% end %>
</ul><br />

<h4>Victorious Comparisons: </h4>
<ul>
  <% @movie.superior_comparisons.each do |comparison| %>
    <li><%= comparison.user.name %> likes it more than <%= comparison.interior_movie.title %></li>
    <% end %>
</ul><br />

<h4>My Haters: </h4>
<ul>
  <% @movie.haters.each do |user| %>
    <li><%= user.name %></li>
    <% end %>
</ul><br />

<h4>Losing Comparisons: </h4>
<ul>
  <% @movie.interior_comparisons.each do |comparison| %>
    <li><%= comparison.user.name %> thinks that <%= comparison.superior_movie.title %>is better</li>
    <% end %>
</ul><br />

<small><%= link_to '<< Back to Movies List', movies_path %></small><br />
<small><%= link_to '<< Back to Users List', users_path %></small>
